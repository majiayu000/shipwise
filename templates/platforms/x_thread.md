# X Thread Template

1. Hook:
   `{{pain_or_result}}`

2. What it is:
   `I built {{project_name}}, {{one_sentence}}.`

3. Demo:
   `{{demo_url}}`

4. Proof:
   `{{proof}}`

5. How to try:
   ```bash
   {{install_command}}
   ```

6. Why it is different:
   `{{differentiation}}`

7. Limitations:
   `{{limitations}}`

8. CTA:
   `Try it here: {{repo_url}}. Feedback I want: {{feedback_request}}.`
